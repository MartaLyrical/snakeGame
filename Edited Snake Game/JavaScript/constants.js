export const numOfBlocks = 400;
export const mySel = (sel) => document.querySelector(sel);
export const mySelAll = (sel) => document.querySelectorAll(sel);
export const numOfRow = Math.sqrt(numOfBlocks);